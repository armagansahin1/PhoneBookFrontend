import { NameStylePipe } from './name-style.pipe';

describe('NameStylePipe', () => {
  it('create an instance', () => {
    const pipe = new NameStylePipe();
    expect(pipe).toBeTruthy();
  });
});
